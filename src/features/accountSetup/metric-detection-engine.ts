// metric-detection-engine.ts
//
// Legacy alias-based metric detection has been retired in favor of the active
// importPolicy.ts + mappingMemory.ts flow. This file is intentionally left as a
// compatibility stub so the old duplicate infrastructure is not silently still in
// the runtime path.
